# Code owners for swimblocks/.github
#
# Every file in this repo carries org-wide policy weight, so the catch-all owner
# below is enough: branch-protection "Require review from Code Owners" gates any
# change to any file through @gavinbee.
#
# Don't add per-file lines unless they need a *different* owner from the default
# — CODEOWNERS evaluates last-match-wins, so duplicating @gavinbee on individual
# paths adds noise without changing behaviour.

*  @gavinbee
